<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.masai.licious">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Licious">
        <activity android:name=".categories.ReadyToCook"></activity>
        <activity android:name=".categories.Mutton">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".categories.Chicken" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".LoginScreenOne" />
        <activity android:name=".NotificationsOffers" />
        <activity android:name=".LocationScreen2" />
        <activity android:name=".LocationScreen" />
        <activity android:name=".categories.Fish" />
        <activity android:name=".LoginScreenTwo" />
        <activity android:name=".SplashScreen"></activity>
    </application>

</manifest>